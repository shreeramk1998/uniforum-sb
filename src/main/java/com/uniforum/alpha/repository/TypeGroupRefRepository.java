package com.uniforum.alpha.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.uniforum.alpha.entity.TypeGroupRef;

public interface TypeGroupRefRepository extends JpaRepository<TypeGroupRef, Long> {
	
}
